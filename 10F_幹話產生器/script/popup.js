let changeStyle = (style) =>{
    if(style == 'light'){
        add.style.backgroundColor = `#000`
        add.style.color = `#fff`
        inputNewQuote.style.color = `#000`
    }else{
        add.style.backgroundColor = `#fff`
        add.style.color = `#000`
        inputNewQuote.style.color = `#fff`
    }

}
